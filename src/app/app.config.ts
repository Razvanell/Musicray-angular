// Define configuration constants
export const appConfig = {
  apiUrl: 'https://api.example.com',
  featureFlags: {
    newUserFlow: true,
    trackIntegration: false
  },
  auth: {
    loginUrl: '/auth/login',
    registerUrl: '/auth/register'
  },
  // Add more configuration as needed
};
